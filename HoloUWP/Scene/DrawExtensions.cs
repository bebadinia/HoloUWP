using StereoKit;

namespace Scene
{
    public static class DrawExtensions
    {
        //public static void Draw(this Model model, Pose pose, float scale)
        //{
          //  model.Draw(Matrix.TRS(pose.position, pose.orientation, new Vec3(scale, scale, scale)));
        //}
    }
}